apiVersion: v1
kind: Secret
metadata:
  name: dnsbl-secrets
  namespace: postal
type: Opaque
stringData:
  # IMPORTANT: Replace these values before applying
  # These are placeholder values - update with actual credentials
  DB_PASSWORD: "CHANGEME_DB_PASSWORD"
  JIRA_API_TOKEN: "CHANGEME_JIRA_TOKEN"
